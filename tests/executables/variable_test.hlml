# Tests variables.

var global;

entrypoint {
  global = 56;
  var local = 17;
  local + global - 45;
  {
    var inner = 5555;
    inner + local * global;
  }
  var another = local // global;
  another;
}
