# Tests radar instruction.

entrypoint {
  var unit;
  mlog::radar_distance(building, 1, unit);                         # radar      any      any      any  distance building 1 unit
  mlog::radar_enemy_distance(building, 1, unit);                   # radar    enemy      any      any  distance building 1 unit
  mlog::radar_enemy_ally_distance(building, 1, unit);              # radar    enemy     ally      any  distance building 1 unit
  mlog::radar_enemy_ally_player_distance(building, 1, unit);       # radar    enemy     ally   player  distance building 1 unit
  mlog::radar_enemy_ally_attacker_distance(building, 1, unit);     # radar    enemy     ally attacker  distance building 1 unit
  mlog::radar_enemy_ally_flying_distance(building, 1, unit);       # radar    enemy     ally   flying  distance building 1 unit
  mlog::radar_enemy_ally_boss_distance(building, 1, unit);         # radar    enemy     ally     boss  distance building 1 unit
  mlog::radar_enemy_ally_ground_distance(building, 1, unit);       # radar    enemy     ally   ground  distance building 1 unit
  mlog::radar_enemy_player_distance(building, 1, unit);            # radar    enemy   player      any  distance building 1 unit
  mlog::radar_enemy_player_attacker_distance(building, 1, unit);   # radar    enemy   player attacker  distance building 1 unit
  mlog::radar_enemy_player_flying_distance(building, 1, unit);     # radar    enemy   player   flying  distance building 1 unit
  mlog::radar_enemy_player_boss_distance(building, 1, unit);       # radar    enemy   player     boss  distance building 1 unit
  mlog::radar_enemy_player_ground_distance(building, 1, unit);     # radar    enemy   player   ground  distance building 1 unit
  mlog::radar_enemy_attacker_distance(building, 1, unit);          # radar    enemy attacker      any  distance building 1 unit
  mlog::radar_enemy_attacker_flying_distance(building, 1, unit);   # radar    enemy attacker   flying  distance building 1 unit
  mlog::radar_enemy_attacker_boss_distance(building, 1, unit);     # radar    enemy attacker     boss  distance building 1 unit
  mlog::radar_enemy_attacker_ground_distance(building, 1, unit);   # radar    enemy attacker   ground  distance building 1 unit
  mlog::radar_enemy_flying_distance(building, 1, unit);            # radar    enemy   flying      any  distance building 1 unit
  mlog::radar_enemy_flying_boss_distance(building, 1, unit);       # radar    enemy   flying     boss  distance building 1 unit
  mlog::radar_enemy_flying_ground_distance(building, 1, unit);     # radar    enemy   flying   ground  distance building 1 unit
  mlog::radar_enemy_boss_distance(building, 1, unit);              # radar    enemy     boss      any  distance building 1 unit
  mlog::radar_enemy_boss_ground_distance(building, 1, unit);       # radar    enemy     boss   ground  distance building 1 unit
  mlog::radar_enemy_ground_distance(building, 1, unit);            # radar    enemy   ground      any  distance building 1 unit
  mlog::radar_ally_distance(building, 1, unit);                    # radar     ally      any      any  distance building 1 unit
  mlog::radar_ally_player_distance(building, 1, unit);             # radar     ally   player      any  distance building 1 unit
  mlog::radar_ally_player_attacker_distance(building, 1, unit);    # radar     ally   player attacker  distance building 1 unit
  mlog::radar_ally_player_flying_distance(building, 1, unit);      # radar     ally   player   flying  distance building 1 unit
  mlog::radar_ally_player_boss_distance(building, 1, unit);        # radar     ally   player     boss  distance building 1 unit
  mlog::radar_ally_player_ground_distance(building, 1, unit);      # radar     ally   player   ground  distance building 1 unit
  mlog::radar_ally_attacker_distance(building, 1, unit);           # radar     ally attacker      any  distance building 1 unit
  mlog::radar_ally_attacker_flying_distance(building, 1, unit);    # radar     ally attacker   flying  distance building 1 unit
  mlog::radar_ally_attacker_boss_distance(building, 1, unit);      # radar     ally attacker     boss  distance building 1 unit
  mlog::radar_ally_attacker_ground_distance(building, 1, unit);    # radar     ally attacker   ground  distance building 1 unit
  mlog::radar_ally_flying_distance(building, 1, unit);             # radar     ally   flying      any  distance building 1 unit
  mlog::radar_ally_flying_boss_distance(building, 1, unit);        # radar     ally   flying     boss  distance building 1 unit
  mlog::radar_ally_flying_ground_distance(building, 1, unit);      # radar     ally   flying   ground  distance building 1 unit
  mlog::radar_ally_boss_distance(building, 1, unit);               # radar     ally     boss      any  distance building 1 unit
  mlog::radar_ally_boss_ground_distance(building, 1, unit);        # radar     ally     boss   ground  distance building 1 unit
  mlog::radar_ally_ground_distance(building, 1, unit);             # radar     ally   ground      any  distance building 1 unit
  mlog::radar_player_distance(building, 1, unit);                  # radar   player      any      any  distance building 1 unit
  mlog::radar_player_attacker_distance(building, 1, unit);         # radar   player attacker      any  distance building 1 unit
  mlog::radar_player_attacker_flying_distance(building, 1, unit);  # radar   player attacker   flying  distance building 1 unit
  mlog::radar_player_attacker_boss_distance(building, 1, unit);    # radar   player attacker     boss  distance building 1 unit
  mlog::radar_player_attacker_ground_distance(building, 1, unit);  # radar   player attacker   ground  distance building 1 unit
  mlog::radar_player_flying_distance(building, 1, unit);           # radar   player   flying      any  distance building 1 unit
  mlog::radar_player_flying_boss_distance(building, 1, unit);      # radar   player   flying     boss  distance building 1 unit
  mlog::radar_player_flying_ground_distance(building, 1, unit);    # radar   player   flying   ground  distance building 1 unit
  mlog::radar_player_boss_distance(building, 1, unit);             # radar   player     boss      any  distance building 1 unit
  mlog::radar_player_boss_ground_distance(building, 1, unit);      # radar   player     boss   ground  distance building 1 unit
  mlog::radar_player_ground_distance(building, 1, unit);           # radar   player   ground      any  distance building 1 unit
  mlog::radar_attacker_distance(building, 1, unit);                # radar attacker      any      any  distance building 1 unit
  mlog::radar_attacker_flying_distance(building, 1, unit);         # radar attacker   flying      any  distance building 1 unit
  mlog::radar_attacker_flying_boss_distance(building, 1, unit);    # radar attacker   flying     boss  distance building 1 unit
  mlog::radar_attacker_flying_ground_distance(building, 1, unit);  # radar attacker   flying   ground  distance building 1 unit
  mlog::radar_attacker_boss_distance(building, 1, unit);           # radar attacker     boss      any  distance building 1 unit
  mlog::radar_attacker_boss_ground_distance(building, 1, unit);    # radar attacker     boss   ground  distance building 1 unit
  mlog::radar_attacker_ground_distance(building, 1, unit);         # radar attacker   ground      any  distance building 1 unit
  mlog::radar_flying_distance(building, 1, unit);                  # radar   flying      any      any  distance building 1 unit
  mlog::radar_flying_boss_distance(building, 1, unit);             # radar   flying     boss      any  distance building 1 unit
  mlog::radar_flying_boss_ground_distance(building, 1, unit);      # radar   flying     boss   ground  distance building 1 unit
  mlog::radar_flying_ground_distance(building, 1, unit);           # radar   flying   ground      any  distance building 1 unit
  mlog::radar_boss_distance(building, 1, unit);                    # radar     boss      any      any  distance building 1 unit
  mlog::radar_boss_ground_distance(building, 1, unit);             # radar     boss   ground      any  distance building 1 unit
  mlog::radar_ground_distance(building, 1, unit);                  # radar   ground      any      any  distance building 1 unit
  mlog::radar_health(building, 1, unit);                           # radar      any      any      any    health building 1 unit
  mlog::radar_enemy_health(building, 1, unit);                     # radar    enemy      any      any    health building 1 unit
  mlog::radar_enemy_ally_health(building, 1, unit);                # radar    enemy     ally      any    health building 1 unit
  mlog::radar_enemy_ally_player_health(building, 1, unit);         # radar    enemy     ally   player    health building 1 unit
  mlog::radar_enemy_ally_attacker_health(building, 1, unit);       # radar    enemy     ally attacker    health building 1 unit
  mlog::radar_enemy_ally_flying_health(building, 1, unit);         # radar    enemy     ally   flying    health building 1 unit
  mlog::radar_enemy_ally_boss_health(building, 1, unit);           # radar    enemy     ally     boss    health building 1 unit
  mlog::radar_enemy_ally_ground_health(building, 1, unit);         # radar    enemy     ally   ground    health building 1 unit
  mlog::radar_enemy_player_health(building, 1, unit);              # radar    enemy   player      any    health building 1 unit
  mlog::radar_enemy_player_attacker_health(building, 1, unit);     # radar    enemy   player attacker    health building 1 unit
  mlog::radar_enemy_player_flying_health(building, 1, unit);       # radar    enemy   player   flying    health building 1 unit
  mlog::radar_enemy_player_boss_health(building, 1, unit);         # radar    enemy   player     boss    health building 1 unit
  mlog::radar_enemy_player_ground_health(building, 1, unit);       # radar    enemy   player   ground    health building 1 unit
  mlog::radar_enemy_attacker_health(building, 1, unit);            # radar    enemy attacker      any    health building 1 unit
  mlog::radar_enemy_attacker_flying_health(building, 1, unit);     # radar    enemy attacker   flying    health building 1 unit
  mlog::radar_enemy_attacker_boss_health(building, 1, unit);       # radar    enemy attacker     boss    health building 1 unit
  mlog::radar_enemy_attacker_ground_health(building, 1, unit);     # radar    enemy attacker   ground    health building 1 unit
  mlog::radar_enemy_flying_health(building, 1, unit);              # radar    enemy   flying      any    health building 1 unit
  mlog::radar_enemy_flying_boss_health(building, 1, unit);         # radar    enemy   flying     boss    health building 1 unit
  mlog::radar_enemy_flying_ground_health(building, 1, unit);       # radar    enemy   flying   ground    health building 1 unit
  mlog::radar_enemy_boss_health(building, 1, unit);                # radar    enemy     boss      any    health building 1 unit
  mlog::radar_enemy_boss_ground_health(building, 1, unit);         # radar    enemy     boss   ground    health building 1 unit
  mlog::radar_enemy_ground_health(building, 1, unit);              # radar    enemy   ground      any    health building 1 unit
  mlog::radar_ally_health(building, 1, unit);                      # radar     ally      any      any    health building 1 unit
  mlog::radar_ally_player_health(building, 1, unit);               # radar     ally   player      any    health building 1 unit
  mlog::radar_ally_player_attacker_health(building, 1, unit);      # radar     ally   player attacker    health building 1 unit
  mlog::radar_ally_player_flying_health(building, 1, unit);        # radar     ally   player   flying    health building 1 unit
  mlog::radar_ally_player_boss_health(building, 1, unit);          # radar     ally   player     boss    health building 1 unit
  mlog::radar_ally_player_ground_health(building, 1, unit);        # radar     ally   player   ground    health building 1 unit
  mlog::radar_ally_attacker_health(building, 1, unit);             # radar     ally attacker      any    health building 1 unit
  mlog::radar_ally_attacker_flying_health(building, 1, unit);      # radar     ally attacker   flying    health building 1 unit
  mlog::radar_ally_attacker_boss_health(building, 1, unit);        # radar     ally attacker     boss    health building 1 unit
  mlog::radar_ally_attacker_ground_health(building, 1, unit);      # radar     ally attacker   ground    health building 1 unit
  mlog::radar_ally_flying_health(building, 1, unit);               # radar     ally   flying      any    health building 1 unit
  mlog::radar_ally_flying_boss_health(building, 1, unit);          # radar     ally   flying     boss    health building 1 unit
  mlog::radar_ally_flying_ground_health(building, 1, unit);        # radar     ally   flying   ground    health building 1 unit
  mlog::radar_ally_boss_health(building, 1, unit);                 # radar     ally     boss      any    health building 1 unit
  mlog::radar_ally_boss_ground_health(building, 1, unit);          # radar     ally     boss   ground    health building 1 unit
  mlog::radar_ally_ground_health(building, 1, unit);               # radar     ally   ground      any    health building 1 unit
  mlog::radar_player_health(building, 1, unit);                    # radar   player      any      any    health building 1 unit
  mlog::radar_player_attacker_health(building, 1, unit);           # radar   player attacker      any    health building 1 unit
  mlog::radar_player_attacker_flying_health(building, 1, unit);    # radar   player attacker   flying    health building 1 unit
  mlog::radar_player_attacker_boss_health(building, 1, unit);      # radar   player attacker     boss    health building 1 unit
  mlog::radar_player_attacker_ground_health(building, 1, unit);    # radar   player attacker   ground    health building 1 unit
  mlog::radar_player_flying_health(building, 1, unit);             # radar   player   flying      any    health building 1 unit
  mlog::radar_player_flying_boss_health(building, 1, unit);        # radar   player   flying     boss    health building 1 unit
  mlog::radar_player_flying_ground_health(building, 1, unit);      # radar   player   flying   ground    health building 1 unit
  mlog::radar_player_boss_health(building, 1, unit);               # radar   player     boss      any    health building 1 unit
  mlog::radar_player_boss_ground_health(building, 1, unit);        # radar   player     boss   ground    health building 1 unit
  mlog::radar_player_ground_health(building, 1, unit);             # radar   player   ground      any    health building 1 unit
  mlog::radar_attacker_health(building, 1, unit);                  # radar attacker      any      any    health building 1 unit
  mlog::radar_attacker_flying_health(building, 1, unit);           # radar attacker   flying      any    health building 1 unit
  mlog::radar_attacker_flying_boss_health(building, 1, unit);      # radar attacker   flying     boss    health building 1 unit
  mlog::radar_attacker_flying_ground_health(building, 1, unit);    # radar attacker   flying   ground    health building 1 unit
  mlog::radar_attacker_boss_health(building, 1, unit);             # radar attacker     boss      any    health building 1 unit
  mlog::radar_attacker_boss_ground_health(building, 1, unit);      # radar attacker     boss   ground    health building 1 unit
  mlog::radar_attacker_ground_health(building, 1, unit);           # radar attacker   ground      any    health building 1 unit
  mlog::radar_flying_health(building, 1, unit);                    # radar   flying      any      any    health building 1 unit
  mlog::radar_flying_boss_health(building, 1, unit);               # radar   flying     boss      any    health building 1 unit
  mlog::radar_flying_boss_ground_health(building, 1, unit);        # radar   flying     boss   ground    health building 1 unit
  mlog::radar_flying_ground_health(building, 1, unit);             # radar   flying   ground      any    health building 1 unit
  mlog::radar_boss_health(building, 1, unit);                      # radar     boss      any      any    health building 1 unit
  mlog::radar_boss_ground_health(building, 1, unit);               # radar     boss   ground      any    health building 1 unit
  mlog::radar_ground_health(building, 1, unit);                    # radar   ground      any      any    health building 1 unit
  mlog::radar_shield(building, 1, unit);                           # radar      any      any      any    shield building 1 unit
  mlog::radar_enemy_shield(building, 1, unit);                     # radar    enemy      any      any    shield building 1 unit
  mlog::radar_enemy_ally_shield(building, 1, unit);                # radar    enemy     ally      any    shield building 1 unit
  mlog::radar_enemy_ally_player_shield(building, 1, unit);         # radar    enemy     ally   player    shield building 1 unit
  mlog::radar_enemy_ally_attacker_shield(building, 1, unit);       # radar    enemy     ally attacker    shield building 1 unit
  mlog::radar_enemy_ally_flying_shield(building, 1, unit);         # radar    enemy     ally   flying    shield building 1 unit
  mlog::radar_enemy_ally_boss_shield(building, 1, unit);           # radar    enemy     ally     boss    shield building 1 unit
  mlog::radar_enemy_ally_ground_shield(building, 1, unit);         # radar    enemy     ally   ground    shield building 1 unit
  mlog::radar_enemy_player_shield(building, 1, unit);              # radar    enemy   player      any    shield building 1 unit
  mlog::radar_enemy_player_attacker_shield(building, 1, unit);     # radar    enemy   player attacker    shield building 1 unit
  mlog::radar_enemy_player_flying_shield(building, 1, unit);       # radar    enemy   player   flying    shield building 1 unit
  mlog::radar_enemy_player_boss_shield(building, 1, unit);         # radar    enemy   player     boss    shield building 1 unit
  mlog::radar_enemy_player_ground_shield(building, 1, unit);       # radar    enemy   player   ground    shield building 1 unit
  mlog::radar_enemy_attacker_shield(building, 1, unit);            # radar    enemy attacker      any    shield building 1 unit
  mlog::radar_enemy_attacker_flying_shield(building, 1, unit);     # radar    enemy attacker   flying    shield building 1 unit
  mlog::radar_enemy_attacker_boss_shield(building, 1, unit);       # radar    enemy attacker     boss    shield building 1 unit
  mlog::radar_enemy_attacker_ground_shield(building, 1, unit);     # radar    enemy attacker   ground    shield building 1 unit
  mlog::radar_enemy_flying_shield(building, 1, unit);              # radar    enemy   flying      any    shield building 1 unit
  mlog::radar_enemy_flying_boss_shield(building, 1, unit);         # radar    enemy   flying     boss    shield building 1 unit
  mlog::radar_enemy_flying_ground_shield(building, 1, unit);       # radar    enemy   flying   ground    shield building 1 unit
  mlog::radar_enemy_boss_shield(building, 1, unit);                # radar    enemy     boss      any    shield building 1 unit
  mlog::radar_enemy_boss_ground_shield(building, 1, unit);         # radar    enemy     boss   ground    shield building 1 unit
  mlog::radar_enemy_ground_shield(building, 1, unit);              # radar    enemy   ground      any    shield building 1 unit
  mlog::radar_ally_shield(building, 1, unit);                      # radar     ally      any      any    shield building 1 unit
  mlog::radar_ally_player_shield(building, 1, unit);               # radar     ally   player      any    shield building 1 unit
  mlog::radar_ally_player_attacker_shield(building, 1, unit);      # radar     ally   player attacker    shield building 1 unit
  mlog::radar_ally_player_flying_shield(building, 1, unit);        # radar     ally   player   flying    shield building 1 unit
  mlog::radar_ally_player_boss_shield(building, 1, unit);          # radar     ally   player     boss    shield building 1 unit
  mlog::radar_ally_player_ground_shield(building, 1, unit);        # radar     ally   player   ground    shield building 1 unit
  mlog::radar_ally_attacker_shield(building, 1, unit);             # radar     ally attacker      any    shield building 1 unit
  mlog::radar_ally_attacker_flying_shield(building, 1, unit);      # radar     ally attacker   flying    shield building 1 unit
  mlog::radar_ally_attacker_boss_shield(building, 1, unit);        # radar     ally attacker     boss    shield building 1 unit
  mlog::radar_ally_attacker_ground_shield(building, 1, unit);      # radar     ally attacker   ground    shield building 1 unit
  mlog::radar_ally_flying_shield(building, 1, unit);               # radar     ally   flying      any    shield building 1 unit
  mlog::radar_ally_flying_boss_shield(building, 1, unit);          # radar     ally   flying     boss    shield building 1 unit
  mlog::radar_ally_flying_ground_shield(building, 1, unit);        # radar     ally   flying   ground    shield building 1 unit
  mlog::radar_ally_boss_shield(building, 1, unit);                 # radar     ally     boss      any    shield building 1 unit
  mlog::radar_ally_boss_ground_shield(building, 1, unit);          # radar     ally     boss   ground    shield building 1 unit
  mlog::radar_ally_ground_shield(building, 1, unit);               # radar     ally   ground      any    shield building 1 unit
  mlog::radar_player_shield(building, 1, unit);                    # radar   player      any      any    shield building 1 unit
  mlog::radar_player_attacker_shield(building, 1, unit);           # radar   player attacker      any    shield building 1 unit
  mlog::radar_player_attacker_flying_shield(building, 1, unit);    # radar   player attacker   flying    shield building 1 unit
  mlog::radar_player_attacker_boss_shield(building, 1, unit);      # radar   player attacker     boss    shield building 1 unit
  mlog::radar_player_attacker_ground_shield(building, 1, unit);    # radar   player attacker   ground    shield building 1 unit
  mlog::radar_player_flying_shield(building, 1, unit);             # radar   player   flying      any    shield building 1 unit
  mlog::radar_player_flying_boss_shield(building, 1, unit);        # radar   player   flying     boss    shield building 1 unit
  mlog::radar_player_flying_ground_shield(building, 1, unit);      # radar   player   flying   ground    shield building 1 unit
  mlog::radar_player_boss_shield(building, 1, unit);               # radar   player     boss      any    shield building 1 unit
  mlog::radar_player_boss_ground_shield(building, 1, unit);        # radar   player     boss   ground    shield building 1 unit
  mlog::radar_player_ground_shield(building, 1, unit);             # radar   player   ground      any    shield building 1 unit
  mlog::radar_attacker_shield(building, 1, unit);                  # radar attacker      any      any    shield building 1 unit
  mlog::radar_attacker_flying_shield(building, 1, unit);           # radar attacker   flying      any    shield building 1 unit
  mlog::radar_attacker_flying_boss_shield(building, 1, unit);      # radar attacker   flying     boss    shield building 1 unit
  mlog::radar_attacker_flying_ground_shield(building, 1, unit);    # radar attacker   flying   ground    shield building 1 unit
  mlog::radar_attacker_boss_shield(building, 1, unit);             # radar attacker     boss      any    shield building 1 unit
  mlog::radar_attacker_boss_ground_shield(building, 1, unit);      # radar attacker     boss   ground    shield building 1 unit
  mlog::radar_attacker_ground_shield(building, 1, unit);           # radar attacker   ground      any    shield building 1 unit
  mlog::radar_flying_shield(building, 1, unit);                    # radar   flying      any      any    shield building 1 unit
  mlog::radar_flying_boss_shield(building, 1, unit);               # radar   flying     boss      any    shield building 1 unit
  mlog::radar_flying_boss_ground_shield(building, 1, unit);        # radar   flying     boss   ground    shield building 1 unit
  mlog::radar_flying_ground_shield(building, 1, unit);             # radar   flying   ground      any    shield building 1 unit
  mlog::radar_boss_shield(building, 1, unit);                      # radar     boss      any      any    shield building 1 unit
  mlog::radar_boss_ground_shield(building, 1, unit);               # radar     boss   ground      any    shield building 1 unit
  mlog::radar_ground_shield(building, 1, unit);                    # radar   ground      any      any    shield building 1 unit
  mlog::radar_armor(building, 1, unit);                            # radar      any      any      any     armor building 1 unit
  mlog::radar_enemy_armor(building, 1, unit);                      # radar    enemy      any      any     armor building 1 unit
  mlog::radar_enemy_ally_armor(building, 1, unit);                 # radar    enemy     ally      any     armor building 1 unit
  mlog::radar_enemy_ally_player_armor(building, 1, unit);          # radar    enemy     ally   player     armor building 1 unit
  mlog::radar_enemy_ally_attacker_armor(building, 1, unit);        # radar    enemy     ally attacker     armor building 1 unit
  mlog::radar_enemy_ally_flying_armor(building, 1, unit);          # radar    enemy     ally   flying     armor building 1 unit
  mlog::radar_enemy_ally_boss_armor(building, 1, unit);            # radar    enemy     ally     boss     armor building 1 unit
  mlog::radar_enemy_ally_ground_armor(building, 1, unit);          # radar    enemy     ally   ground     armor building 1 unit
  mlog::radar_enemy_player_armor(building, 1, unit);               # radar    enemy   player      any     armor building 1 unit
  mlog::radar_enemy_player_attacker_armor(building, 1, unit);      # radar    enemy   player attacker     armor building 1 unit
  mlog::radar_enemy_player_flying_armor(building, 1, unit);        # radar    enemy   player   flying     armor building 1 unit
  mlog::radar_enemy_player_boss_armor(building, 1, unit);          # radar    enemy   player     boss     armor building 1 unit
  mlog::radar_enemy_player_ground_armor(building, 1, unit);        # radar    enemy   player   ground     armor building 1 unit
  mlog::radar_enemy_attacker_armor(building, 1, unit);             # radar    enemy attacker      any     armor building 1 unit
  mlog::radar_enemy_attacker_flying_armor(building, 1, unit);      # radar    enemy attacker   flying     armor building 1 unit
  mlog::radar_enemy_attacker_boss_armor(building, 1, unit);        # radar    enemy attacker     boss     armor building 1 unit
  mlog::radar_enemy_attacker_ground_armor(building, 1, unit);      # radar    enemy attacker   ground     armor building 1 unit
  mlog::radar_enemy_flying_armor(building, 1, unit);               # radar    enemy   flying      any     armor building 1 unit
  mlog::radar_enemy_flying_boss_armor(building, 1, unit);          # radar    enemy   flying     boss     armor building 1 unit
  mlog::radar_enemy_flying_ground_armor(building, 1, unit);        # radar    enemy   flying   ground     armor building 1 unit
  mlog::radar_enemy_boss_armor(building, 1, unit);                 # radar    enemy     boss      any     armor building 1 unit
  mlog::radar_enemy_boss_ground_armor(building, 1, unit);          # radar    enemy     boss   ground     armor building 1 unit
  mlog::radar_enemy_ground_armor(building, 1, unit);               # radar    enemy   ground      any     armor building 1 unit
  mlog::radar_ally_armor(building, 1, unit);                       # radar     ally      any      any     armor building 1 unit
  mlog::radar_ally_player_armor(building, 1, unit);                # radar     ally   player      any     armor building 1 unit
  mlog::radar_ally_player_attacker_armor(building, 1, unit);       # radar     ally   player attacker     armor building 1 unit
  mlog::radar_ally_player_flying_armor(building, 1, unit);         # radar     ally   player   flying     armor building 1 unit
  mlog::radar_ally_player_boss_armor(building, 1, unit);           # radar     ally   player     boss     armor building 1 unit
  mlog::radar_ally_player_ground_armor(building, 1, unit);         # radar     ally   player   ground     armor building 1 unit
  mlog::radar_ally_attacker_armor(building, 1, unit);              # radar     ally attacker      any     armor building 1 unit
  mlog::radar_ally_attacker_flying_armor(building, 1, unit);       # radar     ally attacker   flying     armor building 1 unit
  mlog::radar_ally_attacker_boss_armor(building, 1, unit);         # radar     ally attacker     boss     armor building 1 unit
  mlog::radar_ally_attacker_ground_armor(building, 1, unit);       # radar     ally attacker   ground     armor building 1 unit
  mlog::radar_ally_flying_armor(building, 1, unit);                # radar     ally   flying      any     armor building 1 unit
  mlog::radar_ally_flying_boss_armor(building, 1, unit);           # radar     ally   flying     boss     armor building 1 unit
  mlog::radar_ally_flying_ground_armor(building, 1, unit);         # radar     ally   flying   ground     armor building 1 unit
  mlog::radar_ally_boss_armor(building, 1, unit);                  # radar     ally     boss      any     armor building 1 unit
  mlog::radar_ally_boss_ground_armor(building, 1, unit);           # radar     ally     boss   ground     armor building 1 unit
  mlog::radar_ally_ground_armor(building, 1, unit);                # radar     ally   ground      any     armor building 1 unit
  mlog::radar_player_armor(building, 1, unit);                     # radar   player      any      any     armor building 1 unit
  mlog::radar_player_attacker_armor(building, 1, unit);            # radar   player attacker      any     armor building 1 unit
  mlog::radar_player_attacker_flying_armor(building, 1, unit);     # radar   player attacker   flying     armor building 1 unit
  mlog::radar_player_attacker_boss_armor(building, 1, unit);       # radar   player attacker     boss     armor building 1 unit
  mlog::radar_player_attacker_ground_armor(building, 1, unit);     # radar   player attacker   ground     armor building 1 unit
  mlog::radar_player_flying_armor(building, 1, unit);              # radar   player   flying      any     armor building 1 unit
  mlog::radar_player_flying_boss_armor(building, 1, unit);         # radar   player   flying     boss     armor building 1 unit
  mlog::radar_player_flying_ground_armor(building, 1, unit);       # radar   player   flying   ground     armor building 1 unit
  mlog::radar_player_boss_armor(building, 1, unit);                # radar   player     boss      any     armor building 1 unit
  mlog::radar_player_boss_ground_armor(building, 1, unit);         # radar   player     boss   ground     armor building 1 unit
  mlog::radar_player_ground_armor(building, 1, unit);              # radar   player   ground      any     armor building 1 unit
  mlog::radar_attacker_armor(building, 1, unit);                   # radar attacker      any      any     armor building 1 unit
  mlog::radar_attacker_flying_armor(building, 1, unit);            # radar attacker   flying      any     armor building 1 unit
  mlog::radar_attacker_flying_boss_armor(building, 1, unit);       # radar attacker   flying     boss     armor building 1 unit
  mlog::radar_attacker_flying_ground_armor(building, 1, unit);     # radar attacker   flying   ground     armor building 1 unit
  mlog::radar_attacker_boss_armor(building, 1, unit);              # radar attacker     boss      any     armor building 1 unit
  mlog::radar_attacker_boss_ground_armor(building, 1, unit);       # radar attacker     boss   ground     armor building 1 unit
  mlog::radar_attacker_ground_armor(building, 1, unit);            # radar attacker   ground      any     armor building 1 unit
  mlog::radar_flying_armor(building, 1, unit);                     # radar   flying      any      any     armor building 1 unit
  mlog::radar_flying_boss_armor(building, 1, unit);                # radar   flying     boss      any     armor building 1 unit
  mlog::radar_flying_boss_ground_armor(building, 1, unit);         # radar   flying     boss   ground     armor building 1 unit
  mlog::radar_flying_ground_armor(building, 1, unit);              # radar   flying   ground      any     armor building 1 unit
  mlog::radar_boss_armor(building, 1, unit);                       # radar     boss      any      any     armor building 1 unit
  mlog::radar_boss_ground_armor(building, 1, unit);                # radar     boss   ground      any     armor building 1 unit
  mlog::radar_ground_armor(building, 1, unit);                     # radar   ground      any      any     armor building 1 unit
  mlog::radar_maxhealth(building, 1, unit);                        # radar      any      any      any maxHealth building 1 unit
  mlog::radar_enemy_maxhealth(building, 1, unit);                  # radar    enemy      any      any maxHealth building 1 unit
  mlog::radar_enemy_ally_maxhealth(building, 1, unit);             # radar    enemy     ally      any maxHealth building 1 unit
  mlog::radar_enemy_ally_player_maxhealth(building, 1, unit);      # radar    enemy     ally   player maxHealth building 1 unit
  mlog::radar_enemy_ally_attacker_maxhealth(building, 1, unit);    # radar    enemy     ally attacker maxHealth building 1 unit
  mlog::radar_enemy_ally_flying_maxhealth(building, 1, unit);      # radar    enemy     ally   flying maxHealth building 1 unit
  mlog::radar_enemy_ally_boss_maxhealth(building, 1, unit);        # radar    enemy     ally     boss maxHealth building 1 unit
  mlog::radar_enemy_ally_ground_maxhealth(building, 1, unit);      # radar    enemy     ally   ground maxHealth building 1 unit
  mlog::radar_enemy_player_maxhealth(building, 1, unit);           # radar    enemy   player      any maxHealth building 1 unit
  mlog::radar_enemy_player_attacker_maxhealth(building, 1, unit);  # radar    enemy   player attacker maxHealth building 1 unit
  mlog::radar_enemy_player_flying_maxhealth(building, 1, unit);    # radar    enemy   player   flying maxHealth building 1 unit
  mlog::radar_enemy_player_boss_maxhealth(building, 1, unit);      # radar    enemy   player     boss maxHealth building 1 unit
  mlog::radar_enemy_player_ground_maxhealth(building, 1, unit);    # radar    enemy   player   ground maxHealth building 1 unit
  mlog::radar_enemy_attacker_maxhealth(building, 1, unit);         # radar    enemy attacker      any maxHealth building 1 unit
  mlog::radar_enemy_attacker_flying_maxhealth(building, 1, unit);  # radar    enemy attacker   flying maxHealth building 1 unit
  mlog::radar_enemy_attacker_boss_maxhealth(building, 1, unit);    # radar    enemy attacker     boss maxHealth building 1 unit
  mlog::radar_enemy_attacker_ground_maxhealth(building, 1, unit);  # radar    enemy attacker   ground maxHealth building 1 unit
  mlog::radar_enemy_flying_maxhealth(building, 1, unit);           # radar    enemy   flying      any maxHealth building 1 unit
  mlog::radar_enemy_flying_boss_maxhealth(building, 1, unit);      # radar    enemy   flying     boss maxHealth building 1 unit
  mlog::radar_enemy_flying_ground_maxhealth(building, 1, unit);    # radar    enemy   flying   ground maxHealth building 1 unit
  mlog::radar_enemy_boss_maxhealth(building, 1, unit);             # radar    enemy     boss      any maxHealth building 1 unit
  mlog::radar_enemy_boss_ground_maxhealth(building, 1, unit);      # radar    enemy     boss   ground maxHealth building 1 unit
  mlog::radar_enemy_ground_maxhealth(building, 1, unit);           # radar    enemy   ground      any maxHealth building 1 unit
  mlog::radar_ally_maxhealth(building, 1, unit);                   # radar     ally      any      any maxHealth building 1 unit
  mlog::radar_ally_player_maxhealth(building, 1, unit);            # radar     ally   player      any maxHealth building 1 unit
  mlog::radar_ally_player_attacker_maxhealth(building, 1, unit);   # radar     ally   player attacker maxHealth building 1 unit
  mlog::radar_ally_player_flying_maxhealth(building, 1, unit);     # radar     ally   player   flying maxHealth building 1 unit
  mlog::radar_ally_player_boss_maxhealth(building, 1, unit);       # radar     ally   player     boss maxHealth building 1 unit
  mlog::radar_ally_player_ground_maxhealth(building, 1, unit);     # radar     ally   player   ground maxHealth building 1 unit
  mlog::radar_ally_attacker_maxhealth(building, 1, unit);          # radar     ally attacker      any maxHealth building 1 unit
  mlog::radar_ally_attacker_flying_maxhealth(building, 1, unit);   # radar     ally attacker   flying maxHealth building 1 unit
  mlog::radar_ally_attacker_boss_maxhealth(building, 1, unit);     # radar     ally attacker     boss maxHealth building 1 unit
  mlog::radar_ally_attacker_ground_maxhealth(building, 1, unit);   # radar     ally attacker   ground maxHealth building 1 unit
  mlog::radar_ally_flying_maxhealth(building, 1, unit);            # radar     ally   flying      any maxHealth building 1 unit
  mlog::radar_ally_flying_boss_maxhealth(building, 1, unit);       # radar     ally   flying     boss maxHealth building 1 unit
  mlog::radar_ally_flying_ground_maxhealth(building, 1, unit);     # radar     ally   flying   ground maxHealth building 1 unit
  mlog::radar_ally_boss_maxhealth(building, 1, unit);              # radar     ally     boss      any maxHealth building 1 unit
  mlog::radar_ally_boss_ground_maxhealth(building, 1, unit);       # radar     ally     boss   ground maxHealth building 1 unit
  mlog::radar_ally_ground_maxhealth(building, 1, unit);            # radar     ally   ground      any maxHealth building 1 unit
  mlog::radar_player_maxhealth(building, 1, unit);                 # radar   player      any      any maxHealth building 1 unit
  mlog::radar_player_attacker_maxhealth(building, 1, unit);        # radar   player attacker      any maxHealth building 1 unit
  mlog::radar_player_attacker_flying_maxhealth(building, 1, unit); # radar   player attacker   flying maxHealth building 1 unit
  mlog::radar_player_attacker_boss_maxhealth(building, 1, unit);   # radar   player attacker     boss maxHealth building 1 unit
  mlog::radar_player_attacker_ground_maxhealth(building, 1, unit); # radar   player attacker   ground maxHealth building 1 unit
  mlog::radar_player_flying_maxhealth(building, 1, unit);          # radar   player   flying      any maxHealth building 1 unit
  mlog::radar_player_flying_boss_maxhealth(building, 1, unit);     # radar   player   flying     boss maxHealth building 1 unit
  mlog::radar_player_flying_ground_maxhealth(building, 1, unit);   # radar   player   flying   ground maxHealth building 1 unit
  mlog::radar_player_boss_maxhealth(building, 1, unit);            # radar   player     boss      any maxHealth building 1 unit
  mlog::radar_player_boss_ground_maxhealth(building, 1, unit);     # radar   player     boss   ground maxHealth building 1 unit
  mlog::radar_player_ground_maxhealth(building, 1, unit);          # radar   player   ground      any maxHealth building 1 unit
  mlog::radar_attacker_maxhealth(building, 1, unit);               # radar attacker      any      any maxHealth building 1 unit
  mlog::radar_attacker_flying_maxhealth(building, 1, unit);        # radar attacker   flying      any maxHealth building 1 unit
  mlog::radar_attacker_flying_boss_maxhealth(building, 1, unit);   # radar attacker   flying     boss maxHealth building 1 unit
  mlog::radar_attacker_flying_ground_maxhealth(building, 1, unit); # radar attacker   flying   ground maxHealth building 1 unit
  mlog::radar_attacker_boss_maxhealth(building, 1, unit);          # radar attacker     boss      any maxHealth building 1 unit
  mlog::radar_attacker_boss_ground_maxhealth(building, 1, unit);   # radar attacker     boss   ground maxHealth building 1 unit
  mlog::radar_attacker_ground_maxhealth(building, 1, unit);        # radar attacker   ground      any maxHealth building 1 unit
  mlog::radar_flying_maxhealth(building, 1, unit);                 # radar   flying      any      any maxHealth building 1 unit
  mlog::radar_flying_boss_maxhealth(building, 1, unit);            # radar   flying     boss      any maxHealth building 1 unit
  mlog::radar_flying_boss_ground_maxhealth(building, 1, unit);     # radar   flying     boss   ground maxHealth building 1 unit
  mlog::radar_flying_ground_maxhealth(building, 1, unit);          # radar   flying   ground      any maxHealth building 1 unit
  mlog::radar_boss_maxhealth(building, 1, unit);                   # radar     boss      any      any maxHealth building 1 unit
  mlog::radar_boss_ground_maxhealth(building, 1, unit);            # radar     boss   ground      any maxHealth building 1 unit
  mlog::radar_ground_maxhealth(building, 1, unit);                 # radar   ground      any      any maxHealth building 1 unit
}
